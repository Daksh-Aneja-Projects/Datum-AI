apiVersion: v1
kind: Service
metadata:
  name: {{ include "manufacturing-ai.fullname" . }}
  labels:
    {{- include "manufacturing-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manufacturing-ai.selectorLabels" . | nindent 4 }}